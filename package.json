{
  "name": "pptx-legacy-transform-web-tests",
  "version": "0.0.0",
  "private": true,
  "description": "Jest test harness verifying the rendered HTML output of the dual-edition publish pipeline. Python unittest covers the DITA source and publisher orchestration; this layer asserts on the rendered HTML content.",
  "scripts": {
    "test": "jest"
  },
  "devDependencies": {
    "cheerio": "^1.0.0",
    "jest": "^29.7.0"
  }
}
